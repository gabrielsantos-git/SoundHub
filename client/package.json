{
  "name": "soundhub-client",
  "version": "1.0.0",
  "description": "Frontend do SoundHub para deploy",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.js dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
